#!/bin/sh
set -o nounset
#set -x #DEBUG

. config/config-vars
. bin/functions.sh

require_rootfs_rw_mounted

echo Installing custom casper boot script...
$SUDO install -g root -o root -m 664 \
    custom/casper/casper \
    $ROOTFSMNT_RW/usr/share/initramfs-tools/scripts

echo Installing custom isolinux menu configuration...
$SUDO mkdir -p $ISOMNT_RW/isolinux
$SUDO install -g root -o root -m 664 \
    custom/casper/txt.cfg \
    $ISOMNT_RW/isolinux

exit 0
